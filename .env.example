# Lexis Budget Management System - Environment Configuration
# Copy this file to .env and fill in the actual values for your environment

# ============================================================================
# DEPLOYMENT ENVIRONMENT
# ============================================================================
NODE_ENV=development
PORT=5000

# ============================================================================
# DATABASE & SUPABASE CONFIGURATION
# ============================================================================
# Supabase Project URL (e.g., https://your-project.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (public, safe to expose in browser)
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (SECRET - server-side only, never expose in client)
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Alternative: Direct PostgreSQL connection string (used if Supabase keys not available)
DATABASE_URL=postgresql://user:password@localhost:5432/lexis_budget

# ============================================================================
# SECURITY & SESSION CONFIGURATION
# ============================================================================
# Session secret for express-session (use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-session-secret-change-me-in-production

# Cookie domain for production deployments (e.g., .sdegdaefk.gr)
# Leave empty for local development
COOKIE_DOMAIN=

# ============================================================================
# ENCRYPTION & SECURITY KEYS
# ============================================================================
# AFM (Greek Tax ID) encryption key
# Used for encrypting sensitive AFM data
AFM_KEY=your-afm-encryption-key-change-me

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================
# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ============================================================================
# DEPLOYMENT URLs
# ============================================================================
# Full deployed URL for security headers and CORS (e.g., https://budget.sdegdaefk.gr)
DEPLOYED_URL=

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================
# Session cookie name (defaults to 'sid')
SESSION_COOKIE_NAME=sid

# Number of worker threads (defaults to system CPU count)
WORKER_THREADS=4
